# Dependencies
node_modules/
/.pnp
.pnp.js
.yarn/install-state.gz

# Next.js
.next/
out/
next-env.d.ts
# Rewritten by `next dev` on every start (Next 16.3's agent notice pointing at
# node_modules/next/dist/docs). Tool output, not project documentation — the
# project's own guidance lives in docs/ and README.md.
AGENTS.md
CLAUDE.md

# SST / IaC
.sst/
.open-next/
.terraform/
*.tfstate
*.tfstate.*
# .terraform.lock.hcl is deliberately COMMITTED (HashiCorp guidance: review provider
# upgrades like code). infra/terraform/.gitignore keeps the rest of the Terraform noise out.

# Build output
dist/
build/

# Environment & secrets
.env
.env.*
!.env.example

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS / editor
.DS_Store
Thumbs.db
*.pem
/.idea/*
!/.idea/runConfigurations

# TypeScript
*.tsbuildinfo

# Test / coverage
coverage/
playwright-report/
test-results/
blob-report/
playwright/.cache/
.lighthouseci/

# Uploaded media (local dev only — production media lives in S3)
/media
