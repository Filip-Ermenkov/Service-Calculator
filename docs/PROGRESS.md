# Progress log — bulbau.lu

> Rolling history of what's been built, what broke, what was learned, and what's next. `docs/TECHSPEC.md` is the plan; `docs/FUNCTIONALITY.md` is the target behavior; this document is the record of how far implementation actually is and how it got there. Read this first when picking the project back up, especially in a fresh session with no prior context.

---

## Current state (2026-07-17)

**Phase 0, Phase 1, Phase 1.5, Phase 2 part 1, Phase 2 part 2 (interactive + schema evolution), and the Phase 2 part 2 CI audit gates are complete and live/green.** The **axe-core WCAG 2.2 AA + Lighthouse CI gates** (§6.11/§7B) were built and verified green in CI on 2026-07-18 (their own section below). A fresh session picking this up has **one item left in Phase 2 part 2 — cookie-free analytics** (Plausible/Umami, §6.10) — and then moves to **Phase 3** (the visual calculator/formula builder + live evaluator). See "Immediate next steps".

- **Phase 0** (signed off 2026-07-04): repo scaffolding — Next.js 16.2 + Payload 3.85 on Lambda via OpenNext/SST, Neon Postgres, admin auth, S3 media CRUD, CI.
- **Phase 1** (2026-07-06): mandatory TOTP 2FA (`23a8b7c`) + the full content model — Services/Projects/CareerListings collections and CompanyInfo/LegalInfo globals, EN/FR/DE localization, draft/publish, public-read access control, and the LegalInfo publish gate (`aec6c25`, `41f615f`). All CRUD-usable through the default Payload admin.
- **Phase 1.5** — DB migrations workflow + deploy-secret wiring (`59a9e20`, `60b8a21`; 2026-07-07): the operational layer that made Phase 1 deployable (its own section below).
- **Phase 2, part 1** (built + verified 2026-07-17): the public site wired to the CMS — next-intl URL-based i18n (`/en|/fr|/de`), a CMS-driven shell (Header/Footer from CompanyInfo), and Home / Service detail / Projects / About / Careers / Legal / Privacy pages rendering real content via ISR + on-demand revalidation, with SEO (sitemap/robots/hreflang/JSON-LD) and an accessibility baseline. Replaces the Phase 0 placeholder. **Verified end-to-end**: local `lint` (0 errors) / `typecheck` / `test:int` / `next build` / `test:e2e` all green, and confirmed live on staging — the public site serves real content, and on-demand revalidation works on Lambda (an admin publish appears on the public page within seconds). Its own section below.
- **Phase 2, part 2 — interactive + schema evolution** (built + verified 2026-07-17, `8fd1df9`): clean service **slugs** (`/services/[slug]` replacing `/services/[id]`), the §7 **service-label snapshot** on Projects (retained after the service is deleted — the migrations workflow's first real use "in anger", `20260717_164130_phase2b_slug_service_snapshot`), the **Projects client-side search + category filter**, and confirmation that the **sitewide default disclaimer** is served correctly (incl. the empty-rich-text fallback bug fixed during testing). **Verified end-to-end** on Filip's machine (lint 0 errors / typecheck / test:int / build / test:e2e green, migration `--skip-empty` clean) and **live on staging** (slug URLs resolve, filter works, retain-on-delete holds). Its own section below. **Deferred to the remaining part 2:** analytics + the Lighthouse/axe CI gates.

**Staging cutover complete and verified (2026-07-08)**, confirmed by Filip against this session's end-to-end guide: the staging Neon branch was baselined once (`migrate:fresh`), CI ran `payload migrate` (a no-op on the baseline) then `sst deploy`, and **staging now serves the real Phase 1, not the Phase 0 build**. Verified live on the staging URL — admin panel loads (proving `TOTP_ENCRYPTION_KEY` is wired), first login forces `/admin/totp-setup`, CRUD works across all collections, anonymous `GET /api/services` hides drafts, and a `migrate:create` → push round-trip applied exactly one new migration through CI. **Schema evolution is now a solved, repeatable operation.**

Live staging URL: `https://d2mj4ke0wr57lb.cloudfront.net` (no custom domain yet). Repo: `github.com/Filip-Ermenkov/Service-Calculator`. **Before trusting this section, verify `git log --oneline` and `git status`** — this project's sandbox has had real git/filesystem flakiness (see "Environment quirks").

> **✅ Blocker fully resolved and verified live (2026-07-08):** the "no migrations step on deploy" blocker is closed end-to-end. `src/payload.config.ts` sets `migrationDir` + explicit `push` (dev/CI push on; deployed stages push OFF, migrations only); the initial migration is committed at `src/migrations/`; `.github/workflows/ci.yml`'s `deploy-staging` runs `npm run migrate` (direct/unpooled Neon URL, `NODE_ENV=production`, fail-fast if the URL secret is missing) before `sst deploy`; and the staging Neon branch has been baselined and is now tracking migrations. Local dev and CI tests are unaffected (they push on boot).

**What is NOT done yet (so a fresh session doesn't assume otherwise):**
- **The last Phase 2 part 2 item: cookie-free analytics** (Plausible/Umami — no consent banner per §2.5/§6.10). The other split-off infra item, the **Lighthouse CI + axe-core gates, is now DONE** (2026-07-18, green in CI — its own section below).
- **Follow-ups the audit-gate slice deliberately left open:**
  - **Pre-launch SEO + mobile-legibility polish** (bundle with the perf pass below; a hard gate before launch). The Lighthouse SEO WARN (~0.5–0.58) is only partly the by-design `noindex` — real audits fail too: `meta-description` missing on `/legal` + `/privacy` and empty on `/about` until the CMS is populated (a quick fix), and very likely `legible-font-size` + `tap-targets` from the design's small uppercase labels / tiny language buttons (design-token work that can ripple the visual system — treat like the contrast fix). **First step: read the actual per-audit SEO breakdown** (`npx lighthouse http://localhost:3000/en --only-categories=seo --view`, or add report-artifact upload to CI) so the slice is precisely scoped. Only after this does enabling indexing at launch get SEO to a hard-gateable ≥0.9.
  - **`<img>`→`next/image` perf pass** — currently non-failing lint warnings; doing it lets the Lighthouse **performance** floor be tightened above 0.8.
  - **axe coverage of `/services/[slug]`** — the service-detail template (calculator preview + price box) isn't audited because it 404s on CI's empty DB; add it once CI can seed a published service.
  - (Optional) upload the Lighthouse/Playwright HTML reports as CI artifacts — needs a Dependabot-pinned `actions/upload-artifact` SHA.
- **Phases 3–7**: the visual **Calculator Field/Formula Builder** + live evaluator (Phase 3 — Phase 2 renders the calculator fields as a static preview, the live math is Phase 3); PDF quotes (Phase 4); the DeepL translation pipeline + the `Translations` management surface (Phase 5); contact form + spam protection (Phase 6); hardening / Well-Architected pass (Phase 7).
- **Standing note — repurposed/deleted Phase 0 & Part-1 files:** the old placeholder `src/app/(frontend)/{layout,page}.tsx` (and unused `styles.css`) were *repurposed*, not deleted (the sandbox can't delete on the Windows mount) — `(frontend)` now only serves the `/ → /<locale>` redirect; the real site lives under `src/app/[locale]/`. The old ID-based `src/app/[locale]/services/[id]/` route was **deleted by Filip** when slugs landed (a required manual step — two dynamic siblings `[id]`/`[slug]` can't coexist); after deleting it, `getServiceById`/`getPublishedServiceIds` in `src/lib/content.ts` are unused dead exports, harmless, optionally removable. **Gotcha seen doing this:** deleting a route leaves a stale `.next/types/**/validator.ts` referencing the old route, which fails `tsc`/`build` until `.next` is cleared (`npm run devsafe`, or delete `.next`).
- Carried follow-ups: IAM Access Analyzer pass on the deploy role; an optional CI schema-drift guard; **GitHub repository hardening** (branch protection ruleset on `main` + security settings — Filip had been committing directly to `main`; a guide was provided 2026-07-17); and the client's real legal details (a hard gate on Phase 6, and what makes the Legal Notice / Privacy pages render real content instead of "not yet available"). See "Immediate next steps".

---

## Phase 2, part 1 — public site foundation (built + verified 2026-07-17)

The first user-visible slice: the public site, wired to the CMS, replacing the Phase 0 placeholder. Scope was deliberately bounded to the **foundation + all content pages at current-phase depth** — everything that shares one rendering/i18n/ISR/SEO pattern — while the genuinely separate concerns (live calculator = Phase 3, contact form = Phase 6) and the higher-risk interactive/schema pieces (Projects search + service-label snapshot migration, analytics, CI audit gates) are Part 2. Rationale: the risky, novel engineering is all in the shared foundation (i18n routing composed with the existing Node-runtime `proxy.ts`, ISR/revalidation, SEO, the design-system port); once that's proven, the remaining pages are repetition, and a bounded slice keeps the E2E test guide trustworthy.

### What was built (files in this slice)

- **i18n (next-intl v4.13):** `src/i18n/routing.ts` (`en`/`fr`/`de`, `defaultLocale: en`, `localePrefix: 'always'`), `request.ts` (per-request config + message loading), `navigation.ts` (locale-aware `Link`/`usePathname`/`useRouter`), and `messages/{en,fr,de}.json` — **109 UI-chrome keys, fully trilingual now** (CMS content still falls back to EN until Phase 5; the static chrome doesn't have to wait). `next.config.ts` wraps `withNextIntl(withPayload(...))`.
- **`src/proxy.ts` — the headline integration.** The existing file already owned the Node-runtime TOTP admin gate; next-intl also wants `proxy.ts`. Composed by **path dispatch**: `/admin/*` → the (verbatim) TOTP gate, everything else → `createMiddleware(routing)`. Matcher broadened from `['/admin/:path*']` to next-intl's `['/((?!api|_next|_vercel|.*\\..*).*)']` (covers both branches; `/api` stays excluded). `payloadJwtKey` is still exported for `proxy.int.spec.ts`. This is next-intl's own documented "composing other middlewares" shape.
- **Route structure:** the localized site is a **top-level `src/app/[locale]/`** segment (its own root layout renders `<html lang={locale}>` — the canonical next-intl pattern, and the only place `lang` + static rendering both work). The Phase 0 `src/app/(frontend)/{layout,page}.tsx` were **repurposed** (can't delete on the mount) into a minimal `/ → /<locale>` redirect. Three root layouts now coexist (`[locale]`, `(frontend)`, `(payload)`) — valid because there's no `app/layout.tsx` (Next's documented multiple-root-layouts rule).
- **Shell:** `components/site/Header.tsx` (client — mobile menu state + active-route + a no-JS language switcher that keeps the path and swaps the locale), `Footer.tsx` (server — contact/social from CompanyInfo), `RichText.tsx` (Payload's `@payloadcms/richtext-lexical/react` converter), `JsonLd.tsx`, `icons.tsx`. `app/[locale]/globals.css` ports the approved prototype's design system (admin-only classes dropped); fonts via `next/font` (Inter + Barlow Condensed) → `--font-inter`/`--font-barlow`.
- **Pages (all ISR, `revalidate = 300`):** Home (services grid + JSON-LD `LocalBusiness`), Service detail `/services/[id]` (description, disclaimer, calculator fields as a **static preview** + JSON-LD `Service`), Projects (newest-first grid), About (CompanyInfo rich text + contact), Careers (active listings + empty state), Legal Notice + Privacy Policy (gated on `_status === 'published'`, reusing `findMissingLegalFields` so placeholder legal details can never render), plus a localized `not-found.tsx`.
- **Data layer (`src/lib/content.ts`):** typed Local-API fetchers, all `overrideAccess: false` (the **same** published-only access rule as the REST boundary — the public site can't diverge from the tested policy) and all resilient (a DB failure logs and returns empty, so a build with no DB reachable still succeeds). `mediaProps` resolves uploads; `lexical.ts` extracts card/meta excerpts.
- **ISR revalidation (`src/lib/revalidate.ts`):** `afterChange`/`afterDelete` hooks on the three collections + `afterChange` on both globals call `revalidatePath('/', 'layout')` + per-locale, **double-guarded** (`context.disableRevalidate` for seeds/tests/migrations + try/catch) so it can never turn an admin save or a test into a 500. Time-based ISR is the always-correct safety net underneath.
- **SEO (`src/lib/seo.ts`, `app/sitemap.ts`, `app/robots.ts`):** per-page `generateMetadata` (title/description/canonical/hreflang `x-default`/OG), localized sitemap, robots. Indexing is an **explicit opt-in** (`NEXT_PUBLIC_ALLOW_INDEXING === 'true'`, prod-only) rather than a hostname check — so the dynamic CloudFront staging URL is never accidentally indexed. New optional SST secrets `SiteUrl`/`AllowIndexing` (safe empty defaults) wired in `sst.config.ts`; documented in `.env.example`.
- **CI:** `deploy-staging`'s `sst deploy` now also gets `DATABASE_URL` (the existing unpooled staging secret) so build-time static generation ships **populated** pages, not empty-until-first-revalidate. The build-only value is independent of the Lambda's pooled runtime `DATABASE_URL`.
- **Tests:** `tests/int/publicSite.int.spec.ts` (pure helpers + a real-Postgres check that `getServices()` hides drafts), extended `tests/int/proxy.int.spec.ts` (i18n `/`→locale redirect + Accept-Language, admin gate still fires), rewrote `tests/e2e/frontend.e2e.spec.ts` (shell/i18n/language-switch/per-page render/the legal gate/admin separation — all empty-content-safe so CI's empty DB stays green).

### Decisions worth a fresh session knowing

- **ID-based service URLs, slugs deferred.** `Services` has no slug field; adding one is a schema change (migration), and this slice was deliberately kept migration-free (lower risk, and the sandbox can't run `migrate:create` without the embedded-Postgres workaround). `/services/[id]` works now; slugs land in Part 2 with the Projects snapshot migration. Staging isn't indexed, so the URL change later is safe.
- **`overrideAccess: false` everywhere in the data layer**, not a hand-written `where: { _status: 'published' }`. Reuses the exact access policy `rest.int.spec.ts` already proves, so drafts can't leak through a second, drifting code path.
- **On-demand revalidation is guarded, not clever.** `revalidatePath('/', 'layout')` is the sledgehammer; correctness (not micro-optimization) is the goal for a low-traffic site, and the guards mean the worst case is a harmless no-op.
- **Indexing gate is opt-in, not host-based** (see SEO above) — the safe default is noindex everywhere until launch flips it on (a Phase 7 step).
- **`<img>` over `next/image` for CMS media, for now.** Robust and testable; `next/image` on Lambda/OpenNext (image-optimization function + the deliberately-omitted `sharp`) is exactly the kind of thing that can't be verified in-sandbox. `next.config.ts` already has the `localPatterns` groundwork; switching is a Part-2 perf-pass item (with Lighthouse). The `@next/next/no-img-element` warnings are non-failing.

### Verification (what was actually run, and where)

Same standing rule as every prior slice: **the sandbox can't run this project's real toolchain** (Turbopack/SWC have no sandbox bindings; `next build`, `next dev`, Playwright, and `tsc` against the Windows-mounted `node_modules` don't run here — and the Local API now transitively imports next-intl via the revalidate hooks, so the in-sandbox strip-types harness can't boot Payload either). **CI is the authority; sandbox green is necessary, not sufficient.** What *was* verified in-sandbox:

- **Message catalogs:** all three parse; **`fr`/`de` key sets exactly match `en` (109 keys each)**; ICU placeholders (`{count}`, `{email}`/`{phone}`, `{year}`/`{name}`) consistent across locales.
- **Pure helpers executed:** `lexicalToPlainText` (nesting, whitespace, truncation, non-lexical → '') and `mediaProps` (unset/id-only/no-url → null; populated → props) — all assertions pass.
- **`next-intl@4.13.2`** resolved into `package.json` + `package-lock.json` (via `npm install --package-lock-only`, so `npm ci` stays consistent) with `use-intl`/`@formatjs/intl-localematcher`/`negotiator` present in the lock.
- **Structure/API review** against the current next-intl v4 + Payload 3.85 docs (fetched this session): proxy composition, `[locale]` root-layout rule, `setRequestLocale`/`getMessages`, the Lexical `RichText` converter.

**Then fully verified on Filip's machine + staging (2026-07-17):** `npm run lint` (0 errors), `tsc --noEmit` (clean), `npm run test:int` (real Postgres, all green), `next build` (Turbopack — compiled the whole slice), `npm run test:e2e` (the rewritten public-site suite + the existing admin/TOTP suites), and the staging deploy (site serves real content; on-demand revalidation confirmed working on Lambda). The manual walkthrough (i18n routing, language switch, per-page render, the legal publish gate, admin separation) passed too.

### Issues found during verification (and fixed) — worth a fresh session knowing

The in-sandbox review was necessary but not sufficient (as always); the real toolchain caught six concrete things, all fixed before sign-off:

1. **`tsc` — the one type error.** `findMissingLegalFields(legal)` in the Legal page: `LegalInfo` isn't assignable to `Record<string, unknown>` (no index signature). Fixed by loosening the param type to `Partial<Record<(typeof REQUIRED_LEGAL_FIELDS)[number], unknown>>` (accepts the interface directly, no cast). This was the *only* type error — everything else typechecked first try.
2. **`lint` — one real error, rest warnings.** `not-found.tsx` used `<a href="/">` → `@next/next/no-html-link-for-pages` **error**; switched to the locale-aware `<Link>` (safe — the 404 renders inside the valid-locale layout). Remaining 8 items are **non-failing warnings**: 4 `<img>` (`no-img-element` — `next/image` is a deliberate Part-2 perf item, `next.config.ts` already has the `localPatterns` groundwork) and 4 in the *generated* migration file (unused `payload`/`req` args, inherent to Payload's template). ESLint exits 0 on warnings, so CI's `lint` step passes.
3. **`test:int` — the whole `proxy.int.spec.ts` suite failed to LOAD.** `src/proxy.ts` now imports `next-intl/middleware`, whose compiled ESM does `import … from 'next/server'` (extensionless), which Vitest's resolver can't handle outside Next's bundler (`Cannot find module '…/next/server' … Did you mean "next/server.js"?`). Fixed by `vi.mock('next-intl/middleware', …)` in that test (the admin-gate tests only hit `/admin/*`, which dispatch to the TOTP gate and never call the i18n middleware) and **removing the i18n int assertions** — the real i18n routing only runs against a real Next server, so it's covered by e2e instead. **Lesson:** importing next-intl's middleware into any module a Vitest suite loads drags in `next/server`; keep i18n-middleware assertions in e2e, not int.
4. **`test:e2e` — nav assertion shape.** `expect(page.locator('.nav-links')).toContainText([...])` failed: the array form of `toContainText` matches one element per array item, but `.nav-links` is a single `<ul>`. Fixed to target `.nav-links a` (the 4 anchors) and use the exact label `"About Us"`.
5. **`test:e2e` — environment-fragile legal-gate test.** Asserted the Legal page shows "Not yet available", but Filip's local Neon DB had a *published* `LegalInfo` (from manual testing), so it correctly rendered the real registration fields — proving the gate works, but failing the test's assumption. Fixed to accept **either** valid state (`/Not yet available|RCS Luxembourg number/`).
6. **`test:int` — the pre-existing LegalInfo-gate test broke on a polluted global.** `content.int.spec.ts`'s "publishing incomplete must reject" did `updateGlobal({ data: { _status: 'published', legalName: 'x' } })` and expected a throw. But **globals are singletons and `updateGlobal` MERGES** — on a dev DB where `LegalInfo` was already fully populated (Filip's manual publish), the merge satisfied the gate and the publish went through, failing the "should reject" assertion. Passes in CI (fresh empty DB), fails locally. **Hardened** by passing the other identity fields **explicitly blank** in that assertion, so it tests "publishing with blank required fields is blocked" deterministically, independent of pre-existing global state. Good general lesson: **tests that mutate a singleton global must not assume it starts empty.**

**Two operational notes surfaced (not bugs):**
- **`pg` SSL deprecation warning** on boot (`SSL modes 'prefer'/'require'/'verify-ca' are treated as aliases for 'verify-full'…`) — harmless; comes from a Neon `DATABASE_URL` with `?sslmode=require` and a future `pg` v9 default change. Silence by using `sslmode=verify-full` (identical behaviour with Neon today) if desired.
- **MFA reset runbook** (in case admin TOTP codes stop working — clock drift or a changed `TOTP_ENCRYPTION_KEY`): clear the user's 2FA so the next login forces re-enrollment: `UPDATE users SET totp_enabled=false, totp_secret=NULL, totp_last_time_step=NULL WHERE email='…';` (run against the DB in `.env`'s `DATABASE_URL`), then log in with the password → `/admin/totp-setup` → scan a fresh QR.

**First-run gotcha for a fresh session:** `npm ci` must pick up the committed lockfile — if you see "Cannot find module 'next-intl'", `package.json` and `package-lock.json` weren't committed together. And `npm run devsafe` (clean `.next` + restart) is required after any `proxy.ts` change — Next never hot-reloads the proxy.

---

## Phase 2, part 2 — interactive Projects + schema evolution (built + verified 2026-07-17)

The first slice to touch the schema since Phase 1, and so the **migrations workflow's first real use "in anger"** (Phase 1.5 built it; this proved it end-to-end on a real change). Deliberately scoped to the schema-bearing + user-visible core of Phase 2b — slugs, the service-label snapshot, the Projects search/filter, and the disclaimer — while the two cross-cutting infra items (analytics, Lighthouse/axe CI) were split into a follow-up slice to keep this one's E2E guide trustworthy. Commit `8fd1df9`.

### What was built (files in this slice)

- **Clean service slugs (`/services/[slug]`).** New `src/lib/slug.ts`: `formatSlug` (lowercase, accent-folding via `normalize('NFKD')` + `\p{Diacritic}` strip, hyphenate, trim) and a reusable `slugField()` (unique, indexed, sidebar, `beforeValidate` hook that auto-fills from the title but stays editable). Non-localized — one canonical slug per service across all locales (hreflang alternates differ only by the `/xx` prefix). Added to `Services`. New route `src/app/[locale]/services/[slug]/page.tsx` replaces `[id]/` (Filip deleted the old folder — see the standing note above). `src/lib/content.ts` gained `getServiceBySlug` + `getPublishedServiceSlugs`; `sitemap.ts`, the Home card links, and the service page's canonical/`hreflang`/JSON-LD `url` all use the slug.
- **Service-label snapshot on Projects (§7).** New non-localized `serviceName` text field (read-only in admin), kept in sync by a `beforeChange` collection hook `syncServiceNameSnapshot` (`src/collections/Projects.ts`) that copies the linked service's default-locale title onto the project on every save. The projects→services FK is `ON DELETE set null`, so deleting a service nulls the relationship cleanly while `serviceName` persists — the project keeps its category label with no dangling id. The single-value (EN) snapshot is intentional: per-locale category labels fall back to EN everywhere until Phase 5, so a canonical label is coherent and far simpler than a localized snapshot synced across three locales.
- **Projects client-side search + category filter.** New `src/lib/projects.ts` (pure, testable: `filterProjects`, `projectCategories`, the `ProjectCard` type) and `src/components/site/ProjectsBrowser.tsx` (client component). The server (`projects/page.tsx`) pre-maps each `Project` into a lightweight `ProjectCard` (plain strings only — no Lexical shipped to the client), sorted newest-first; the client filters instantly (title/description keyword + exact category, combinable). Categories are derived from the cards themselves, so a deleted service's category only remains a filter option while projects still carry its snapshotted label — exactly §7. Progressive enhancement: the grid still renders server-side. The CSS (`.filter-bar` etc.), the `Search` icon, and the i18n keys (`searchPlaceholder`/`allCategories`/`emptyTitle`/`emptyBody`) had already been scaffolded in Part 1, so this was pure wiring.
- **Sitewide default disclaimer (§3.3).** Confirmed the existing trilingual `disclaimerDefault` message fallback (with live `CompanyInfo` phone/email injected) is the sitewide default — deliberately *not* an admin-editable global field, which would be EN-only until Phase 5 whereas the message catalog is already FR/DE. The service page shows a service's own disclaimer when present, else the default.
- **The migration.** `src/migrations/20260717_164130_phase2b_slug_service_snapshot.{ts,json}` — generated by `npm run migrate:create` on Filip's machine (the sandbox can't boot the config to generate: the Local API now transitively imports next-intl → `next/server`, which the strip-types harness can't load — see Part 1). Adds `services.slug` (+ `_services_v.version_slug`), `projects.service_name` (+ `_projects_v.version_service_name`), the `services_slug_idx` unique index, with a **hand-added backfill** (English-title → slug with id-suffix on collision; relationship → `service_name`) inserted before the unique index. `down()` reverses cleanly.
- **Tests.** `tests/int/publicSite.int.spec.ts` gained pure tests (`formatSlug`, `filterProjects`, `projectCategories`) and a real-Postgres test (slug auto-generation resolvable via `getServiceBySlug`; the snapshot populated on create; **the snapshot RETAINED after the service is deleted**, the relationship no longer resolving, and the deleted slug no longer resolving). `payload-types.ts` updated for the two new fields.

### Decisions worth a fresh session knowing

- **Slug is non-localized, unique, auto-from-title but editable.** Matches Payload's own documented slug pattern (a `beforeValidate` field hook + `formatSlug`, `index: true`, no DB reads in the field hook — collision handling left to the unique index). One slug per document across locales keeps hreflang simple.
- **The snapshot is a collection `beforeChange` hook, not a field hook.** Field hooks must stay deterministic/side-effect-free (Payload guidance); a denormalized snapshot legitimately needs an async `findByID`, which belongs in a collection hook (sharing `req`'s transaction, best-effort so a mid-delete lookup can't block a save).
- **`ON DELETE set null` is what makes retain-on-delete clean.** Because the FK nulls the relationship on delete (rather than restricting or leaving a dangling id), the display logic is simply "live title if the relationship still populates, else the `serviceName` snapshot" — no special-casing.
- **Slugs now, before any indexing.** Staging is `noindex` (Part 1's opt-in gate), so changing service URLs from ids to slugs is free of SEO cost — the correct time to do it.

### Verification (what was run, and where)

Same standing rule (sandbox can't run the real toolchain; CI/Filip's machine is the authority). **In-sandbox:** the pure helpers (`formatSlug`, `filterProjects`, `projectCategories`, and the empty-vs-filled Lexical disclaimer check) executed green via Node's `--experimental-strip-types`. **On Filip's machine:** `npm run lint` (0 errors; the only warnings are the known `<img>` perf items + Payload's migration-template unused-arg warnings), `tsc --noEmit` (clean, after clearing a stale `.next`), `npm run test:int` (green, incl. the new snapshot/retain tests), `npm run build` (Turbopack), `npm run test:e2e` (green), and `migrate:create --skip-empty` reporting **no drift** (the migration fully captures the schema). **Live on staging:** slug URLs resolve, the Projects filter works, and retain-on-delete holds.

### Issues found during testing (and fixed) — worth a fresh session knowing

1. **Emptied rich-text ≠ null — the disclaimer fallback bug.** Clearing a service's `disclaimer` in the admin doesn't store `null`; Lexical stores an *empty document* (`{ root: { children: [empty paragraph] } }`), a truthy object. So `service.disclaimer ? …` stayed true and the page rendered an empty `<RichText>` instead of falling back to the default notice. **Fix:** gate on real text — `hasOwnDisclaimer = !!disclaimer && lexicalToPlainText(disclaimer).trim().length > 0`. **General lesson:** never test a Payload rich-text field for presence with a truthiness check; an emptied field is a non-empty object. Use `lexicalToPlainText(...).trim()`.
2. **`push` ≠ `migrate` — the local backfill gotcha (not a bug).** After deleting a service locally, a *pre-existing* project lost its label. Cause: local dev runs Drizzle `push` (schema sync on boot) which **does not run migrations**, so the migration's backfill `UPDATE` never populated `serviceName` for rows created before this slice — they stayed `NULL`, and on delete there was nothing to fall back to. New/edited projects are fine (the hook), and **on staging this doesn't happen** because CI runs `payload migrate` (backfill included). To reconcile a local DB: re-save each project once (fires the hook) or run the backfill `UPDATE` by hand. **General lesson:** local `push` never applies migration `up()` bodies — any data backfill in a migration only runs on deployed stages (or via an explicit local `migrate` against a non-prod DB), so pre-existing local rows won't reflect it.
3. **Deleting a route leaves stale generated types.** After deleting `[id]/`, `tsc`/`build` failed on `.next/**/types/validator.ts` still importing the removed `[id]/page.js`. **Fix:** clear `.next` (`npm run devsafe`, or delete the folder) so Next regenerates the route-type validators. Harmless, but confusing if you don't know Next caches per-route type validators.

---

## Phase 2, part 2 — CI audit gates: axe-core (a11y) + Lighthouse (built + verified green in CI 2026-07-18)

The first of the two cross-cutting infra items split out of Phase 2b. Replaces the standing `ci.yml` TODO (§6.11/§7B) with two automated regression gates. Deliberately scoped to the gates + the real defects they surfaced; **cookie-free analytics is the remaining Phase 2b item.**

### What was built (files in this slice)

- **axe-core WCAG 2.2 AA gate — `tests/e2e/accessibility.e2e.spec.ts`** (new). Uses `@axe-core/playwright` inside the existing Playwright suite (zero new infra — reuses the `npm run dev` webServer). Iterates all six EN pages + the FR/DE homes; asserts **zero serious/critical** violations (moderate/minor logged for triage, not blocking — a common high-signal axe-in-CI posture). Runs as part of `npm run test:e2e`, so it gates on every PR/push. **Sampling is intentional:** FR/DE subpages are omitted (shell/tokens are locale-independent — full EN + cross-locale home smoke catches both shared and locale-specific issues); `/services/[slug]` is omitted because it 404s on CI's empty DB (documented follow-up).
- **Lighthouse CI — `lighthouserc.cjs`** (new) + a **`Lighthouse CI` step** in `ci.yml`'s `verify` job. Audits a **production** build (`lhci autorun` boots `npm run start`) across 4 URLs × 3 runs (median). Ordered **before** `test:e2e` in `ci.yml` because the e2e step's `next dev` webServer overwrites the `.next` production build in place (a later `next start` would then fail with "no production build"). `throttlingMethod: 'devtools'` (applied, not simulated) + `chromeFlags: '--no-sandbox'`. Reports to `.lighthouseci/` (gitignored). **Calibrated hard gates** (proven 12/12 in the first CI run): accessibility ≥0.9, best-practices ≥0.9, performance ≥0.8. **SEO is a WARN by design** (see decisions). Core Web Vitals are tracked WARNs.
- **Dependencies** (`package.json` / `package-lock.json`): `@axe-core/playwright@4.12.1`, `axe-core@4.12.1`, `@lhci/cli@0.15.1`, an `lhci` script, and an **`overrides: { tmp: ^0.2.7 }`** — `@lhci/cli` transitively pulls a `tmp` with a **high-severity** path-traversal advisory (GHSA-52f5-9888-hmc6 / GHSA-ph9p-34f9-6g65) that would trip CI's `npm audit --audit-level=high`; forcing the patched `tmp` keeps that gate strict rather than weakening it. `.gitignore` gets `.lighthouseci/`.
- **Real accessibility debt fixed** (the gate caught genuine pre-existing issues in the ported prototype design system, all in `src/app/[locale]/globals.css` + `src/lib/seo.ts`):
  - **`color-contrast` (WCAG 1.4.3):** brand orange `#E05A00`→**`#BF4C00`** (was ~3.7:1 as button fill / eyebrow / link / tag / active-lang — now ~4.9:1, verified with exact WCAG math); a scoped `--orange-light` (`#FF6D1F`, ~7:1) for orange accents **on dark sections** (the base orange is only ~4:1 on near-black); muted greys `g500/g600`→**`g400`** on the dark header/footer/price-box (were 2.5–4.2:1); muted text →**`g600/g700`** on the pale empty-state/careers panels.
  - **`document-title` (WCAG 2.4.2):** the home pages (`/en`, `/fr`, `/de`) rendered **no `<title>`** — `pageMetadata()` returned `title: undefined`, which *overrides* the layout's `title.default` instead of inheriting it. Fixed by **omitting the `title` key** when a page has none (also handles OG title).

### Decisions worth a fresh session knowing

- **Two complementary gates, not one.** axe is the authoritative **a11y** hard gate (catches the ~30–40% of WCAG that tooling can, at serious/critical); Lighthouse covers **perf/SEO/best-practices** regressions. Lighthouse's own a11y category is left as a soft signal (redundant with axe).
- **CI (Linux) is the authoritative Lighthouse runner.** On Windows, `lhci` deterministically crashes at the very end with `EPERM` in `chrome-launcher`'s temp-dir cleanup (a known chrome-launcher-on-Windows bug, *after* the report is generated) — not fixable from our config. The Linux CI runner has no such issue. Also seen (non-fatal, both OSes): `LanternError: cycle detected` from LH 12.6.1's trace-insights engine — logged, then the run continues and produces results. To eyeball numbers locally on Windows despite the teardown crash: `npx lighthouse http://localhost:3000/en --output=html --output-path=./lh-report.html --chrome-flags="--headless"` (the report writes before the crash). Same standing rule as always: **CI is the authority.**
- **SEO stays a WARN — and the ~0.5–0.58 baseline is only PARTLY by design.** The biggest hit is the intentional `noindex` outside production (`NEXT_PUBLIC_ALLOW_INDEXING` opt-in — §6.11 / `src/lib/seo.ts`; `is-crawlable` is turned **off** since its failure is deliberate). But a post-hoc code review found **real audits fail underneath it too**: `meta-description` is missing on `/legal` and `/privacy` (no description passed) and empty on `/about` until the CMS is populated (that's why `/about` scored 0.5 vs 0.58 elsewhere), and `legible-font-size` / `tap-targets` are very likely failing given the design's small (0.65–0.75rem) uppercase labels and tiny language buttons. **So enabling indexing at launch will NOT alone lift SEO to 0.9** — the meta-description + mobile-legibility debt must be fixed first (the "pre-launch SEO/mobile polish" follow-up). **Lesson (worth repeating):** don't attribute a low composite score to one cause without reading the per-audit breakdown — the same discipline the contrast fixes needed.
- **Ratchet, don't guess.** The gates landed as all-WARN first, the first CI run established the real baseline (a11y/best-practices/performance all passed 12/12 with margin; only SEO warned), then the stable categories were flipped to `error` at calibrated thresholds. Setting error thresholds before observing CI would have been a flaky gate.
- **Never truthiness-test a design token's fix — compute it.** Every contrast change was verified against the exact WCAG relative-luminance formula (target 4.5:1 for the small/bold text involved) before trusting it; the brand orange needed two shades (a dark one for light backgrounds, `--orange-light` for dark ones) because one colour can't clear 4.5:1 against both white and near-black.

### Verification (what was run, and where)

Same standing rule (sandbox can't run the real toolchain; CI/Filip's machine is the authority). **In-sandbox:** `lighthouserc.cjs` parses to the right shape; `ci.yml` is valid YAML with the step correctly ordered before `test:e2e`; the lockfile resolves with `npm audit --audit-level=high` **clean** (the `tmp` override works); all contrast fixes verified with exact WCAG math. **On Filip's machine:** `npm run lint` (0 errors), `npm run typecheck` (clean), `npm run test:e2e` incl. the axe suite **8/8 green** (after the contrast + title fixes — the gate correctly failed first, caught the debt, then passed). **Green in CI (Linux):** all jobs pass, and the `Lighthouse CI` step completed 4 URLs × 3 runs with only the by-design SEO WARN — confirming the a11y/best-practices/performance hard gates pass 12/12.

### Issues found during testing (and fixed) — worth a fresh session knowing

1. **The axe gate did its job — it failed first, on real debt.** The initial run flagged pervasive `color-contrast` (the whole prototype orange + dark-surface greys) and the missing home `<title>`. These were pre-existing, not introduced here. A second round caught one more contrast case the first fix missed (`.eyebrow` on **dark** CTA sections — the single darkened orange clears 4.5:1 on white but only hits 4.0:1 on near-black, so dark sections needed `--orange-light`). **Lesson:** a design token used on both light and dark backgrounds generally needs two accessible shades.
2. **`title: undefined` overrides, not inherits.** Returning `title: undefined` from a page's `generateMetadata` clobbers the layout's `title.default`; omit the key to inherit. (Only the home pages passed no title, so only they lost their `<title>`.)
3. **`next dev` clobbers the production build.** Lighthouse (`next start`) must run before the e2e step (`next dev`) in CI, or `.next` is a dev build and `next start` fails — hence the step ordering.
4. **A high-severity transitive vuln rode in with `@lhci/cli`.** `tmp` ≤0.2.5 (path traversal) would have failed `npm audit --audit-level=high`; pinned via `overrides` to keep the gate strict. **Lesson:** adding a dev tool can breach an existing supply-chain gate — check `npm audit` after, and prefer a targeted `override` over loosening the gate.
5. **Lighthouse-on-Windows is not a clean local gate.** Deterministic `EPERM` at chrome-launcher teardown (Windows-only) + intermittent non-fatal `LanternError` (LH 12.6.1). Resolved by treating CI (Linux) as authoritative, per the project's standing rule.

---

## Phase 1, part 1 — TOTP 2FA (built 2026-07-06)

Payload has no native 2FA, so this is custom code layered on top of Payload's own email/password auth, per `docs/TECHSPEC.md` §6.6. `docs/FUNCTIONALITY.md` §5.1 treats 2FA as **mandatory, not optional**.

### What was built (files in this slice)

- `src/lib/totp/` — the crypto/OTP toolkit, all Node-runtime:
  - `keys.ts` — one root secret (`TOTP_ENCRYPTION_KEY`, separate from `PAYLOAD_SECRET` for key separation) → two HKDF-derived subkeys (one for AES-256-GCM encryption, one for HMAC signing).
  - `crypto.ts` — AES-256-GCM encrypt/decrypt of the TOTP secret at rest (`iv:authTag:ciphertext`, base64).
  - `otp.ts` — `otplib` wrappers (SHA-1/6-digit/30s defaults, matching mainstream authenticator apps); `epochTolerance: 30` (±1 window) and `afterTimeStep` replay protection.
  - `stepUpToken.ts` — plain HMAC-SHA256 "second-factor-verified" token (`uid`, `exp`), 2h TTL matching Payload's session; `timingSafeEqual` compare.
  - `requestHelpers.ts` — parse/verify the step-up cookie from headers or Payload's cookie map; build set/clear `Set-Cookie` values.
  - `rateLimit.ts` — Upstash Redis sliding-window limiter (5/5min) when `UPSTASH_REDIS_REST_URL/TOKEN` are set; an in-memory fallback for local/CI (same "real service in prod, local stand-in for dev/CI" pattern as S3Mock; not safe multi-instance, warns if it ever activates in prod).
  - `qr.ts` — QR data-URL for the enrollment screen.
- `src/collections/Users.endpoints.ts` — custom endpoints under `/api/users/totp/*`: `setup` (mint secret + QR; re-enroll requires current step-up), `enable` (confirm a code, flip `totpEnabled`, set step-up cookie), `verify` (per-login second factor, sets step-up cookie, user+IP rate-limited), `disable` (requires step-up **and** password re-check), `status`.
- `src/access/requireTotpVerified.ts` — access wrapper: denies unless there's a `req.user`, `totpEnabled`, **and** a valid step-up cookie for that user id; then delegates to the wrapped access fn. **This is the security boundary.**
- `src/proxy.ts` — Next.js 16 Node-runtime request interceptor (the `proxy.ts` convention that replaced `middleware.ts` in Next 16) gating `/admin/:path*`: a password-only session hitting a protected admin route is redirected to `/admin/totp-verify`. UX/defense-in-depth; verifies the `payload-token` JWT itself (see the key-derivation bug below).
- `src/components/admin/` — `TotpSetupView`/`TotpSetupForm`, `TotpVerifyView`/`TotpVerifyForm` (new Payload admin root views at `/admin/totp-setup` and `/admin/totp-verify`), and `BeforeDashboardTotpGate` (a `beforeDashboard` redirect gate). Registered in `payload.config.ts` and `src/app/(payload)/admin/importMap.js` (regenerated, not hand-edited).
- `src/collections/Users.ts` — hidden, non-API-readable `totpSecret`/`totpEnabled`/`totpLastTimeStep` fields; the five endpoints; `requireTotpVerified`-wrapped access; Payload's own `maxLoginAttempts: 5` / `lockTime: 10m` for the password step.
- `src/collections/Media.ts` — writes (`create`/`update`/`delete`) now 2FA-gated; public `read` unchanged (the public site fetches media directly).
- Env/CI/docs: `TOTP_ENCRYPTION_KEY` + Upstash vars added to `.env.example` and CI (`ci.yml`); README gained a 2FA section; `payload.config.ts` wires the views/components; `payload-types.ts` regenerated.
- Tests: `tests/int/totp.int.spec.ts` (crypto round-trip, replay protection, rate-limit fallback, access wrapper, field-hiding), `tests/int/proxy.int.spec.ts` (the proxy's gate logic in isolation), `tests/e2e/totp.e2e.spec.ts` (full enrollment + new-session flows); `tests/helpers/seedUser.ts` gained `seedUnenrolledTestUser`/`seedEnrolledTestUser`; `tests/helpers/login.ts` completes the TOTP step.

### Two real bugs surfaced during testing (both worth a fresh session knowing)

1. **Proxy verified the session JWT against the wrong key — the headline bug.** `src/proxy.ts` verified the `payload-token` with the **raw** `PAYLOAD_SECRET`. Payload does **not** sign with the raw value: on init it derives `crypto.createHash('sha256').update(config.secret).digest('hex').slice(0, 32)` (see `node_modules/payload/dist/index.js`, `BasePayload` init) and signs/verifies with `TextEncoder().encode(...)` of **that**. So every real token failed with `signature verification failed`; `verifiedPayloadUserId` returned `undefined`; the proxy fell through to `NextResponse.next()` and never redirected — the symptom was a password-only session landing on `/admin/collections/users` and seeing an empty "Nothing found" list (the access wrapper correctly denied the data; only the redirect UX was missing). The integration test masked it because it hand-signed its test tokens with the raw secret too — test and buggy proxy agreed with each other while both disagreed with reality. **Fix:** `payloadJwtKey()` in `proxy.ts` reproduces Payload's exact derivation and is **exported and reused by the int test**, so the two can't drift again. **Lesson:** the one piece of custom code re-deriving Payload's own behavior is exactly where reality diverged — from a confidently-worded but unverified comment ("does not hash or transform"). Verify such assumptions against the installed source, and never let a test re-implement the very thing it's meant to check.
   - **Diagnostic red herring worth not repeating:** this was first mis-attributed to Turbopack "not running" the proxy, because `.next/.../middleware-manifest.json` was empty. That file is empty *by design* for a **Node-runtime** proxy — those register in `functions-config-manifest.json`, not `middleware-manifest.json`. A brief switch of `dev` to `--webpack` was a wrong turn (it only added slow-compile timeouts) and was reverted; `dev`/`devsafe` stay on Turbopack. What actually pinned it was temporary `console.warn` instrumentation in the proxy: the dev-server log showed `jwtVerify FAILED: signature verification failed`, which is unambiguous. When a Node-runtime proxy "doesn't fire," instrument it and read the server console rather than reasoning about manifests.
2. **TOTP replay-window flake in the e2e suite (a test bug, not a code bug).** The "new session" test reused the account the enrollment test had *just* enrolled. When its login code fell in the same 30-second TOTP window whose time-step enrollment had already consumed, otplib's replay protection (correctly) rejected it — a ~50–70%-of-runs timeout depending on where the window boundary fell. The 2FA logic is right; the test was unrealistic (real "new session" logins happen long after enrollment). **Fix:** `seedEnrolledTestUser` seeds a dedicated, already-enrolled account with an untouched time-step; the new-session test uses that, which also removes a hidden test-1→test-2 ordering dependency.

### Verification
- `npm run test:int`: green (both new int files; `proxy.int.spec.ts` now signs with the derived key via the shared `payloadJwtKey`).
- `npm run test:e2e`: **now green in CI.** The `deploy-staging` job that shipped Phase 1 `needs: verify`, which runs the full e2e suite — so all 6 passed there (the replay-flake fix included), plus Filip's earlier manual redirect check. `npm run devsafe` (clean `.next` + restart) is required after any `proxy.ts` change — Next never hot-reloads the proxy file.

---

## Phase 1, part 2 — content model (built 2026-07-06)

Completes Phase 1: all the collections and globals the rest of the app builds on, wired for EN/FR/DE localization and draft/publish, usable through the default Payload admin.

### What was built (files in this slice)

- `src/collections/Services.ts` — localized `title`/`description`/card fields, `heroImage`/`cardImage` uploads, `orderable: true` (drag order = Home-page card order, via Payload's fractional-index ordering), `versions.drafts`, and the calculator *data*: a `calculatorFields` array (localized `label`/option labels only; `fieldKey`/`type`/`unitPrice`/`sign`/`required` shared across locales) + a `formula` JSON field. The visual builder over these is Phase 3 (`TECHSPEC.md` §6.4); a service with zero calculator fields is a valid published state (§7).
- `src/collections/Projects.ts` — localized `title`/`description`, `photo`, `completionDate` (`defaultSort: '-completionDate'` → newest first, §3.2), `service` relationship, `versions.drafts`. The §7 "retain the service label even after the service is deleted" requirement is a Phase 2 concern (a denormalized snapshot, added when the Projects-page filter is built) — noted in-file, not built here.
- `src/collections/CareerListings.ts` — localized `title`/`description`, `photo`, `orderable`, and an explicit `status` select (`active`/`archived`) rather than drafts: §5.5 models this as a visibility toggle with Archive/Restore, not a draft→publish authoring flow.
- `src/globals/CompanyInfo.ts` — `email`/`phone`/`facebookUrl`/`instagramUrl` + localized `aboutUsContent`; public `read`, 2FA-gated `update`; no drafts (changes are immediate per §5.6).
- `src/globals/LegalInfo.ts` — legal identity fields + localized `privacyPolicyContent`, `versions.drafts`, and the **§6.9 publish gate**: the five identity fields are `required: true` (Payload blocks publishing them empty while still allowing incomplete *drafts*), **plus** an authoritative `beforeValidate` hook that throws one clear, aggregated error if `_status === 'published'` with any of them blank. The pure checker `findMissingLegalFields` is exported and unit-tested.
- `src/access/publicRead.ts` — `readPublishedOrVerified` / `publicReadWhen(where)`: read access that returns `true` for a *fully 2FA-verified* admin (sees drafts/archived) and a query constraint (`_status: published`, or `status: active` for careers) for everyone else — anonymous public **and** password-only sessions. Deliberately mirrors the three checks in `requireTotpVerified` (the write boundary) so a password-only session can never see drafts through the read path either.
- `src/payload.config.ts` — registers the three collections + two globals; localization expanded from `['en']` to `en`/`fr`/`de` (`defaultLocale: 'en'`, `fallback: true`) **now**, so localized fields have their final shape and FR/DE going live in Phase 5 needs no schema migration.
- `src/payload-types.ts` — regenerated (now includes `services`/`projects`/`career-listings`/`company-info`/`legal-info`).
- `tests/int/content.int.spec.ts` — Local-API integration coverage (see Verification).
- `tests/int/rest.int.spec.ts` — **HTTP-boundary** coverage: invokes the real Payload REST route handler (`REST_GET(config)`) with hand-built `Request`s + cookies, asserting the actual `/api/services` and `/api/career-listings` responses hide drafts/archived from anonymous **and** password-only sessions while a verified-admin cookie sees drafts. Added after a browser `GET /api/services` (from a logged-in, TOTP-verified admin) appeared to leak drafts — it was the correct "verified admin sees everything" path, but the confusion was legitimate and the public HTTP path deserved its own explicit test. Also asserts the **localization fallback** at the serve layer: `?locale=de` returns the EN title for an untranslated field (`localization.fallback: true`), while `?locale=de&fallback-locale=none` returns it empty — the counterpart to the admin editor deliberately showing raw (empty) per-locale values, which surfaced as a second point of confusion during manual testing.

### Decisions worth a fresh session knowing

- **Native localization for all three locales from day one**, not just EN. Enabling FR/DE later would be a data-structure change on every localized field (Payload warns existing data is lost when toggling `localized`), so paying that cost now — while the tables are empty — is the cheap, correct time. FR/DE simply fall back to EN until Phase 5 populates them.
- **`orderable: true` over a manual `order` field.** Payload's built-in fractional-index ordering is the current best practice (verified against the 3.x docs at build time) and gives drag-and-drop in the admin for free; a hand-rolled integer `order` was the older pattern.
- **Careers uses `status`, Services/Projects use drafts.** Different requirements: services/projects are authored-then-published content (drafts + preview fit); a job listing is just shown or hidden (a select fits, and needs no version history).
- **`Translations` deferred to Phase 5** (see "What is NOT done yet"). This is a deliberate scoping call, recorded in `TECHSPEC.md` §5.
- **Publish gate = `required: true` + a hook**, not one or the other: `required` gives clean per-field admin errors and the drafts-bypass-required behaviour we want; the hook guarantees the gate (with one clear message) even if a `required` flag is ever removed, which is what §6.9 literally asks for ("a validation hook prevents…").

### The migrations gap (the headline finding)

This slice is the first to add tables, and doing so surfaced that **the deploy path has no schema-migration step** — it is not a bug in this slice; it's a pre-existing gap that only a schema-changing slice could reveal. Local dev/CI are unaffected (Payload pushes on boot when `NODE_ENV !== 'production'`); only the Lambda staging deploy was blocked. **→ Resolved in Phase 1.5 (below), and the staging cutover has since run — staging is live on Phase 1.**

### Verification (what was actually run, and where)

Sandbox constraints (documented under "Environment quirks") mean the normal toolchain (`npm run generate:types`, `vitest`, `next build`) can't run against the Windows-mounted `node_modules` directly — its native binaries (esbuild/SWC) are Windows-only, and Turbopack has no sandbox bindings. So verification was done on a **sandbox-local copy** of `src`/`tests` with a fresh Linux resolution path:

- **Typecheck — clean (`tsc --noEmit`, exit 0)** across the new collections, globals, access helper, config, and `content.int.spec.ts`, **after** regenerating `payload-types.ts`. (Before regenerating, the only errors were the stale generated types not knowing the new slugs — expected; they all cleared on regeneration.)
- **Runtime integration — 16/16 assertions pass** against a **real Payload boot on a real Postgres** (Postgres 18 via `embedded-postgres`; Payload's TypeScript loaded through Node 22's native `--experimental-strip-types` with a small `@/`-path resolve hook, so no esbuild was needed — Payload/`pg`/Drizzle are pure JS). Covered: EN/FR per-locale storage + DE→EN fallback; the draft/published read gate (public blocked, verified admin allowed, public visible after publish); careers active vs archived; public `CompanyInfo`; and the full `LegalInfo` publish gate (blocked incomplete → allowed as draft → publishes when complete → readable publicly). The committed `tests/int/content.int.spec.ts` mirrors these exactly and will run in CI's real vitest+Postgres job.
- **HTTP boundary — 7/7 assertions pass** against the **real REST route handler** (`REST_GET(config)`, the same one Next serves at `/api/*`), invoked in-process with constructed `Request`s and a real `payload-token` JWT from `payload.login`: anonymous `GET /api/services` and `/api/career-listings` return only published/active; a verified-admin cookie sees drafts; a password-only cookie (stolen password, no TOTP step-up) is treated exactly like the public. `tsc` on `tests/int/rest.int.spec.ts` is clean. This closes the one coverage gap surfaced this session — the tests now cover the public read gate at both the access-rule layer (Local API) **and** the real HTTP layer.
- **`payload-types.ts` was regenerated via Payload's own `generateTypes(config)`** (deterministic from config, identical to `npm run generate:types`) and copied into the repo. `importMap.js` is **unchanged** and needs no regeneration — this slice adds no custom admin components (all fields are built-in types).
- **CI caught a real environment bug the in-sandbox harness structurally could not (now fixed).** On the first CI run, `rest.int.spec.ts` failed with `TypeError: payload must be an instance of Uint8Array` from `payload.login()`. Root cause: the int suite runs under Vitest's **jsdom** environment (`vitest.config.mts`), and Vitest's jsdom setup replaces the global `Uint8Array` with jsdom's own copy, which breaks `jose`'s `instanceof Uint8Array` check during JWT signing (a documented jose+vitest-jsdom issue — panva/jose#671, vitest#5183). Only `rest.int.spec.ts` hits it because it's the only int test that signs a JWT (`payload.login`). **Fix:** a `/** @vitest-environment node */` docblock at the top of `rest.int.spec.ts` — these are server-side HTTP-API tests with no DOM, so `node` is both correct and the documented remedy. **Lesson for future sessions:** the sandbox verification harness loads TS via Node's `--experimental-strip-types` and therefore runs in a **pure Node** environment — it cannot reproduce Vitest-jsdom-specific behavior (global overrides, `instanceof` across realms). For anything sensitive to the test environment, CI is the authority; treat sandbox green as necessary, not sufficient.
- **Not run in-sandbox, validated by CI on push:** `next build` (needs the Linux Next/SWC binary) and the Playwright e2e suite (no new public pages yet — Phase 2). Filip should let CI confirm both, and can re-run `npm run test:int` locally to see `content.int.spec.ts` green in the real harness.

**Environment quirk hit again:** mid-slice, the Linux side of the mount served a **truncated** copy of `payload.config.ts` (125 lines) while the authoritative Windows-side file (via the `Read`/`Edit` tools, and git) was complete and correct (151 lines). This is the same Windows↔Linux sync flakiness noted below — the fix was to reconstruct the sandbox test copy from authoritative content (heredoc) and trust `Read`/`Edit` for the deliverable, exactly as "Environment quirks" prescribes. The committed files are correct; the truncation was only ever the sandbox's stale view.

---

## Phase 1.5 — DB migrations workflow + deploy-secret wiring (built 2026-07-07)

The content-model slice surfaced a blocker (no migrations step on deploy) and, while fixing it, a second latent gap (2FA runtime secrets never wired into `sst.config.ts`). Both block the *same* "next staging deploy," so they're handled together here. This slice adds no product features — it's the operational layer that lets the already-built Phase 1 actually and safely reach a deployed stage.

### What was built (files in this slice)

- `src/payload.config.ts` — `postgresAdapter` now sets `migrationDir: path.resolve(dirname, 'migrations')` and an **explicit** `push: process.env.NODE_ENV !== 'production'` (Payload's own default, made explicit so the dev-push/prod-migrate boundary is documented and can't drift). `prodMigrations` is deliberately **not** set — running migrations at Lambda init would fire on every cold start and let concurrent cold starts race on DDL; on serverless, migrations belong in CI, once per deploy.
- `src/migrations/20260707_153821_initial.{ts,json}` + `src/migrations/index.ts` — the initial full-schema migration (Payload-generated), covering the entire current schema: **33 tables, 15 enums**, with `up` and `down`. Includes Users/Media/Services/Projects/CareerListings/CompanyInfo/LegalInfo, all `*_locales` tables, the `_v` version tables for the drafts-enabled collections (services/projects/legal_info — and correctly *none* for careers-by-status or company_info), the nested calculator-field/option arrays, and Payload's system tables (`payload_migrations`, `payload_locked_documents(+_rels)`, `payload_preferences(+_rels)`, `payload_kv`).
- `package.json` — `migrate`, `migrate:create`, `migrate:status`, `migrate:fresh` scripts (thin `payload` CLI wrappers, matching the existing `payload` script's `cross-env` style).
- `sst.config.ts` — three new `sst.Secret`s injected into the `Web` Lambda's env: `TotpEncryptionKey` (**required** — `src/lib/totp/keys.ts` throws without it, so a missing key would break the admin panel on first load; making it a no-default secret means `sst deploy` fails loudly rather than shipping broken 2FA), and `UpstashRedisRestUrl`/`UpstashRedisRestToken` (**optional**, default `''` ⇒ the rate limiter's in-memory fallback; set real values before production, where multiple instances make in-memory unsafe).
- `.github/workflows/ci.yml` — a `Run database migrations (staging)` step in `deploy-staging`, before `sst deploy`: `npm run migrate` with `NODE_ENV=production` (push off), `DATABASE_URL` = the **direct/unpooled** Neon URL from a new `STAGING_DATABASE_URL_UNPOOLED` GitHub Environment secret, and a throwaway non-empty `PAYLOAD_SECRET` (migrate does DDL, not token signing — deliberately not duplicating the real secret into GitHub). Fail-closed: a bad migration stops the job and nothing deploys. The future `deploy-production` TODO now notes it needs the same migrate-first step.
- Docs: `docs/TECHSPEC.md` (new §10.5, §13 risk marked resolved + the new secrets-gap risk, §10.2/§12 touch-ups), `README.md` (a "Database migrations" section + the new deploy secrets), `.env.example` (the pooled-vs-direct note).

### Decisions worth a fresh session knowing

- **Push in dev/CI, migrate on deployed stages** — Payload's own recommended split, kept (not "push:false everywhere"). CI's `verify` job keeps pushing on boot against its ephemeral Postgres, so the test suite is untouched; only real stages are migrations-only. Having committed migrations present does **not** affect push mode (push ignores the migration files), so `test:int`/`test:e2e` stay green.
- **Migrate in CI, not `prodMigrations`** — the serverless-correct choice (see above), and what Payload's docs recommend for anything that isn't a long-running container.
- **Neon: migrations use the DIRECT (unpooled) URL** — DDL breaks through Neon's PgBouncer pooler (transaction pooling). Runtime stays pooled; only the CI migrate step uses the direct URL. This is the single most likely thing to get wrong, so it's called out in the workflow comment, README, `.env.example`, and TECHSPEC §10.5.
- **`TotpEncryptionKey` required, Upstash optional** — encodes the actual severity: no TOTP key = broken admin (hard-fail the deploy); no Upstash = degraded-but-working rate limiting (tolerated pre-launch).

### Verification (what was actually run, and where)

Validated end-to-end against a **real Postgres** in the sandbox (Postgres 17 via `embedded-postgres`), driving Payload's own migration APIs programmatically:

- **Generate** — `payload.db.createMigration()` against an empty DB produced the initial migration: **33 `CREATE TABLE`, 15 `CREATE TYPE`, with a `down()`**. Table inventory matches the documented design exactly (drafts→`_v`; careers/company_info→none; every localized collection→`_locales`).
- **Apply** — `payload.db.migrate()` applied it cleanly; all 33 tables present; one `payload_migrations` row (`20260707_153821_initial`, batch 1).
- **Idempotency** — a second `migrate()` was a no-op (still one row).
- **Completeness** — a second `createMigration(--skip-empty)` detected **no** changes and wrote no file, proving the committed migration fully captures the config's schema (no drift).
- **Config/adapter** — `postgresAdapter` accepts `{ pool, migrationDir, push }` without error; the full config boots (`getPayload` init) in `NODE_ENV=production`/push-off mode. `package.json` (Read-tool authoritative view) and `ci.yml` (YAML) parse clean.
- **Not run in-sandbox, deferred to CI/Filip's toolchain** (per the standing "sandbox green is necessary, not sufficient; CI is the authority" rule): `tsc --noEmit`, `next build`, the real `payload` CLI (its `tsx`/esbuild loader needs a Linux binary the Windows-mounted `node_modules` doesn't have — see below), and everything that needs Neon/AWS credentials (the staging baseline + first migrated deploy).

**Update (2026-07-08) — now verified live on staging.** Every deferred item above has run: CI's `verify` job is green (`tsc`, `next build`, `test:int`, `test:e2e`); the staging Neon branch was baselined with `migrate:fresh`; and `deploy-staging` ran `payload migrate` (no-op on the baseline) then `sst deploy` successfully. One real bug was caught and fixed along the way — the first `deploy-staging` after committing failed at the migrate step with `ECONNREFUSED ::1:5432`, because `STAGING_DATABASE_URL_UNPOOLED` wasn't set yet, so an empty `DATABASE_URL` made `pg` fall back to localhost. Fixed two ways: setting the secret (the one-time cutover), and adding a fail-fast guard to the CI step (`60b8a21`) so a missing/empty URL now prints a clear, actionable error instead of a cryptic connection refusal. Staging now serves Phase 1, and a follow-up `migrate:create` → push confirmed CI applies exactly one new migration per schema change. **The workflow is operational, not just designed.**

### Sandbox constraints hit (and how they were worked around)

- **The `payload` CLI can't load the TS config in-sandbox** — it uses `tsx`→esbuild, and `@esbuild/linux-x64` isn't in the Windows-mounted `node_modules` (the same "native binaries are Windows-only" quirk from Phase 0). Worked around exactly as the content-model session did: load the config with Node 22's native `--experimental-strip-types` + a small `@/`-alias/extensionless resolve hook, and call `createMigration`/`migrate` on `payload.db` directly. One wrinkle: Payload's generated migration imports `MigrateUpArgs`/`MigrateDownArgs` as *values* (`import { MigrateUpArgs, MigrateDownArgs, sql }`), which `--strip-types` can't elide (only real `tsx`/esbuild does), so the sandbox apply-test patched that one import line in a scratch copy; **the committed file keeps Payload's original import** (correct for CI/Filip's toolchain).
- **`bwrap --unshare-pid --die-with-parent`** — every bash call is its own PID namespace, so **no process survives between calls** (background/`setsid`/`nohup` all die with the call). But the on-disk `pgdata` persists, so the pattern is: warm the cluster (`initdb`) once, then reuse the data dir in later calls (start is ~0.4s, no initdb). Also note `pkill -f postgres` **self-matches** the calling shell (its command line contains "postgres") and kills the call — use `pkill -x postgres` or, since nothing survives a call anyway, don't bother.
- **Stale mount reads** — after editing `package.json` via the Edit tool, the Linux side served a truncated pre-edit copy (2473 bytes, unterminated mid-string), which broke Node's module resolution for any in-sandbox eval. The Read tool (authoritative Windows view) confirmed the file was correct; the adapter-options check was rerun mount-independently (importing `@payloadcms/db-postgres` by absolute path). Same Windows↔Linux flakiness as always — trust Read/Edit/Write.

### What is NOT done (so a fresh session doesn't assume otherwise)

- ~~The one-time staging baseline + first migrated deploy~~ — **done (2026-07-08)**; staging is live on Phase 1 (see the Verification update above).
- **A CI schema-drift guard** (fail the `verify` job if someone changes a collection without generating a migration) — still not done. A good hardening for the "periodic maintenance only" end-state, but deliberately deferred: it's scope beyond this slice and has CI edge cases better added and observed on their own. Carried to "Immediate next steps".

---

## Media upload/delete test coverage — closed (2026-07-04, follow-up to Phase 0)

Phase 0 signed off with one flagged gap: media upload/delete through S3 was only ever manually verified. `tests/int/media.int.spec.ts` now creates a Media doc via the Local API, confirms the object actually lands in S3 via a direct `HeadObjectCommand` (not just Payload's DB row), then deletes it and confirms it's gone — against Adobe's **S3Mock** (Apache-2.0, purpose-built for this), chosen over MinIO/LocalStack after research found both had discontinued their free/CE container images (Oct 2025 / Mar 2026). Writing it surfaced three genuine bugs: a Vitest file-parallelism race against the shared ephemeral Postgres (`fileParallelism: false`), config drift between the test's own S3 client and `payload.config.ts` (share config, don't reimplement), and S3Mock's incomplete CORS support breaking browser `clientUploads` (now `clientUploads: !process.env.S3_ENDPOINT` — off against S3Mock, on for real AWS). Full detail lived in prior revisions of this file; the standing lesson is that "current best practices" must be re-checked at implementation time, not trusted from when a plan was written.

---

## How Phase 0 went (the Lambda spike)

Scaffolding (Next.js 16.2 + Payload 3.85 + Postgres/Drizzle + SST v4/OpenNext + GitHub Actions) went roughly as planned; getting Payload to actually serve on Lambda took five real, non-obvious fixes, none of which invalidated the architecture:
1. OpenNext copies externalized packages (`pg`, `pino`) into the Lambda bundle via symlinks that Windows can't reliably produce as Linux symlinks → **deploys go through CI (Ubuntu) only, never native Windows.**
2. `sharp` is excluded from the main server bundle by OpenNext and ships arch-specific binaries that don't cross-compile (x64 CI → arm64 Lambda) → omitted entirely (nothing in Media uses it; it's now also removed from `package.json`, keeping deps honest).
3. Media uploads need an explicit storage adapter (`@payloadcms/storage-s3`) — SST linking a bucket only wires IAM, it doesn't make Payload use it.
4. `admin/importMap.js` is generated, not static — regenerate (`npm run generate:importmap`) after any collection/field/plugin that contributes admin UI; silently stale otherwise.
5. IAM's `aws:RequestedRegion` doesn't scope global services (CloudFront/IAM/Route 53) → CloudFront needed its own unconditioned statement.

---

## Decisions made along the way (also reflected in TECHSPEC.md)

- **Repo structure**: single app at repo root, not a monorepo; `infra/aws/` holds only the one-time OIDC bootstrap that must exist before SST/CI can run.
- **GitHub Actions ↔ AWS**: OIDC federation (short-lived assumed role), scoped to the exact repo **and** the `staging` GitHub Environment.
- **IAM deploy policy**: broad-but-region-scoped Allow + explicit Deny on high-risk actions; **tighten with IAM Access Analyzer before reusing for a `production` role** (still a tracked follow-up).
- **S3Mock over MinIO/LocalStack** for S3 integration testing (license/distribution changes made both non-viable).
- **2FA (this slice)**: mandatory (`FUNCTIONALITY.md` §5.1); Payload password auth + a custom TOTP step-up cookie; `requireTotpVerified` access wrapper is the boundary, `proxy.ts`/views are UX; `TOTP_ENCRYPTION_KEY` is a **separate** secret from `PAYLOAD_SECRET` (key separation); rate limiting via Upstash with an in-memory local/CI fallback; the proxy must reproduce Payload's own JWT key derivation (see the bug above).
- **docker-compose Postgres host port**: committed as `5431` (mapped to the container's `5432`) to avoid clashing with a system Postgres; `.env.example` line 2 now matches. If this ever feels wrong as a shared default, the idiomatic alternative is a gitignored `docker-compose.override.yml` for personal ports — noted, not done.

---

## Environment quirks worth knowing (specific to this Cowork/Claude sandbox, not the app)

- The Linux sandbox's view of the Windows-mounted repo has intermittent caching problems: `git status`/`git diff`/`cat` sometimes show stale or truncated content. **Trust the Windows-native `Read`/`Edit`/`Write` tools over bash; cross-verify with `git show HEAD:<path>`.** When bash's copy of a file looks stale, rewrite it via a heredoc (`cat > path <<'EOF' ... EOF`) from content confirmed by `Read`.
- **Do not run `git add`/`commit`/`push` from the sandbox** — a stale `.git/index.lock` once could not be removed (`rm` returns `Operation not permitted` on the Windows mount). Filip commits via his own tooling (GitHub Desktop). The same permission quirk means the sandbox **cannot delete files** on the mount — e.g. `smoke-test-tmp.mjs` (a harmless leftover, untracked) had to be left for Filip to delete manually.
- `npm install`/`sst install`/large builds can exceed a single sandbox command's time limit; retrying converges.
- **Turbopack cannot run in this sandbox** — only WebAssembly SWC bindings are available, and Turbopack requires native bindings (`Turbopack is not supported on this platform`). For any in-sandbox `next build`/`next dev` reproduction, use `--webpack`. Filip's real machine (Windows) uses Turbopack, which is the Next 16 default for both `dev` and `build`; keep that distinction in mind when reproducing bundler-adjacent behavior (it's why the Turbopack red herring above was hard to rule out from the sandbox alone).

---

## Immediate next steps

Phase 0, Phase 1, the migrations workflow (Phase 1.5), **Phase 2 part 1 (public site foundation)**, and **Phase 2 part 2 (interactive + schema evolution)** are done and **live on staging** (both verified 2026-07-17). What's next, in priority order:

1. **Finish Phase 2 part 2 — cookie-free analytics (the last item).** The immediate next slice. **Cookie-free analytics** (Plausible or self-hosted Umami — no consent banner per §2.5/§6.10; add the script sitewide in the `[locale]` root layout, gated to the production stage only so staging traffic isn't counted). Same workflow: research current best practices → implement → in-conversation E2E guide → sign off → update these docs. *(The other split-off item, the CI Lighthouse + axe-core gates, is done — see the section below.)* After analytics, **Phase 3** (visual calculator/formula builder + live evaluator) is the next feature phase.

   **Two small follow-ups carried from the audit-gate slice:** the `<img>`→`next/image` perf pass (then tighten the Lighthouse performance floor above 0.8), and adding `/services/[slug]` to the axe gate once CI can seed a published service.

2. **GitHub repository hardening (do this soon — near-zero cost, guards this repo's specific risks).** All work has been committed **directly to `main`** with no branch protection, and `main` auto-runs DB migrations + `sst deploy` on every push. Add a **Ruleset** on `main`: require a PR before merging (0 required approvals is fine for a solo dev — it just forces the CI-green + no-direct-push flow), require the `verify` status check to pass and the branch to be up to date, block force-pushes, and restrict deletions + require linear history (protects the migration/deploy history from a rewrite that would desync the migration chain). Also enable Dependabot alerts + secret-scanning **push protection**, and add branch/environment restrictions on the `staging` GitHub Environment. A full step-by-step guide was given to Filip on 2026-07-17.

3. **IAM Access Analyzer pass** on the staging deploy role, now that it has real CloudTrail history (carried from Phase 0; staging has run several real deploys). Replace the broad region-scoped Allow in `infra/aws/github-actions-deploy-policy.json` with the generated least-privilege policy before this pattern is reused for a `production` role. The CI migrate step assumes no AWS role (it talks to Neon directly), so it adds no IAM surface.

4. **(Hardening, optional) CI schema-drift guard** — add a step to the `verify` job that runs `migrate:create --skip-empty` against the ephemeral Postgres and fails if it writes a file, catching a future collection change that forgot its migration. Worth adding before the "periodic-maintenance-only" end-state. (This slice's migration was verified drift-free manually with exactly this command — automating it is the natural next step.)

5. **A `deploy-production` job — deliberately not yet.** It belongs with Phase 7 (hardening), once there's a custom domain and the app is feature-complete. When added it mirrors `deploy-staging` (migrate-then-deploy, production DIRECT URL) behind a GitHub Environment manual-approval gate, and is **migrations-only, never push**.

6. **Client legal details** (legal form, RCS Luxembourg number, VAT number, registered address) — still outstanding. The `LegalInfo` publish gate (`TECHSPEC.md` §6.9) is built and enforced, so the page **cannot** be published until these are entered; a hard gate on Phase 6 production sign-off.
